
<!-- container-->
<div class="container">
  <h1 class="my-4 text-center text-lg-left">LeaderBoard</h1>
  <table>
  <thead>
    <tr>
      <th>Player</th>
      <th>No of Game</th>
      <th>Aaverage Score</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @players.each do |player| %>
     <tr>
      <td><%= player.name %></td>
      <td><%= player.participations.count %></td>
      <td><%= (player.participations.pluck(:score).sum / player.participations.pluck(:score).count).round%></td>
    </tr>
  <%end%>
  </tbody>
</table>
